import { styled } from "@mui/system";
import { VariantButton } from "../../../Button";

export const VoteCardButton = styled(VariantButton)({
    minWidth: "80px",
    maxWidth: "100px",
    fontSize: "14px",
    minHeight: "40px",
    margin: "0px"
})